Neuroglancer tasks
==================

This includes a set of tasks that are run as background processes. This 
is done so the user doesn't have to wait when clicking an action. The 
background processes are controlled by the supervisord system controller.

****

.. automodule:: neuroglancer.tasks
   :members:
